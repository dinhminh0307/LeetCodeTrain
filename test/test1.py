class Solution:
    def reverseString(self, s: str) -> str:
        store = s.split(" ")
        store.reverse()
        return ' '.join(store)
sol = Solution
print(sol.reverseString(sol, "UNLOCK HUMAN POTENTIAL"))